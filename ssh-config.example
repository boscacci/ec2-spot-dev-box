# ~/.ssh/config snippet for iac-dev-box
#
# After `terraform apply`, grab the public IP from the output and paste it
# into HostName below.  Then you can just: ssh dev-box
#
# IMPORTANT: ForwardAgent yes means your local SSH keys are available on the
# remote box — this is how it can git clone your private repos without ever
# storing your private key on the instance.

Host dev-box
  HostName <PUBLIC_IP_FROM_TERRAFORM>
  User ec2-user
  IdentityFile ~/.ssh/<YOUR_KEY_PAIR>.pem
  ForwardAgent yes
  StrictHostKeyChecking accept-new
  # Keep the connection alive through idle periods
  ServerAliveInterval 60
  ServerAliveCountMax 3
